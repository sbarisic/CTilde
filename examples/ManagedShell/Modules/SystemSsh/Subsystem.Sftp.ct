namespace System.Ssh;

internal static class SshSubsystemFactory
{
    internal static ISshSubsystem Create(string name, string root)
    {
        if (name == "sftp")
            return new SftpSession(root);
        return null;
    }
}
