using System;

namespace Examples.TCan485Failure;

public sealed class Box
{
    public int Value;
}

public static class Program
{
    [Export("ctilde_add")]
    public static int Add(int left, int right)
    {
        return left + right;
    }

    [EntryPoint]
    public static void Main()
    {
        Console.WriteLine("CTILDE_ESP_FAILURE_TEST");
        Box box = null;
        Console.WriteLine(box.Value);
    }
}
