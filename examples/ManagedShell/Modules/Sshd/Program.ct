using System;
using System.Ssh;

internal static class Program
{
    [EntryPoint]
    public static int Main(string[] args)
    {
        if (args.Length != 0)
            return 2;
        return SshDaemon.Run(SshDaemon.ManagedShellDefaults());
    }
}
